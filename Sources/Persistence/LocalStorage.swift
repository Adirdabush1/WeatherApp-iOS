import Foundation

class LocalStorage {
    func saveWeather(_ weather: Weather, for city: City) {
        // Placeholder לשמירה ב-UserDefaults/CoreData
    }
    
    func loadWeather(for city: City) -> Weather? {
        return nil
    }
}
